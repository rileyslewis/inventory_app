version: '3.8'

services:

  fastapi:
    build: ./server
    container_name: fastapi
    ports:
      - "8080:8080"
    command:  uvicorn backend.main:app --reload --workers 1 --host 0.0.0.0 --port 8080
    environment:
      - DATABASE_URL=sqlite:///./sql_app.db
    restart: always
    tty: true
    
    
  frontend:
    build: ./app
    command: yarn dev
    volumes:
      - ./app:/app
    working_dir: /app
    ports:
      - "3000:3000"
    environment:
      - HOST=0.0.0.0
    container_name: frontend
    restart: always
    

networks:
    default:
        driver: bridge
